# TESTING.md

## Required checks

Validate repository structure, YAML syntax, shell syntax, documentation links, and version metadata. Security-sensitive workflow changes require manual review and must demonstrate that write permissions, force-push, and automatic merge are not enabled without an explicit approved use case.

Do not claim success unless the relevant checks were actually run.
